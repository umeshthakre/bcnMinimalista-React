import { REDUCEPRODUCTS } from "../shared/reduceproducts";

export const ReduceProducts = (state = REDUCEPRODUCTS, action) => {
    switch (action.type) {
        default:
            return state;
    }
}